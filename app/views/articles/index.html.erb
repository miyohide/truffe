<%# @side_bar_template_name = 'shared/side_bar' %>

<div id="news">
  <% unless @articles.blank? %>
    <ul id="news_list">
      <%= render @articles %>
    </ul>
    <%= paginate @articles %>
  <% else %>
    <div id="no_articles">現在、ニュースはありません。</div>
  <% end %>
</div>
